from axiolock._compile.persistence import Picklable


class Flag(Picklable):
    """
    A class that represents a compilation flag. It is a picklable object that can be used to store and retrieve the state of a compilation process.
    """
    ...